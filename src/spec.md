# Specification

## Summary
**Goal:** Fix the products page to correctly display all products from the backend.

**Planned changes:**
- Debug and fix the getAllProducts query in ProductsPage.tsx to successfully fetch products
- Verify and fix the backend getAllProducts method in main.mo to return all products with required fields
- Add comprehensive error logging throughout the data flow from backend to frontend rendering
- Ensure the actor instance is correctly initialized and connected to the deployed backend canister

**User-visible outcome:** Products will display correctly on the products page with images, names, prices, and functional add-to-cart buttons.
